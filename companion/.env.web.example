# 网页与 Songloft 插件双端模式：复制为 .env 后替换密码。
PANEL_USERNAME=bluezadmin
PANEL_PASSWORD=请替换为管理密码

# 允许可信局域网访问网页；同机插件也可通过 127.0.0.1 访问。
# 绝不要把 8088 暴露到 WAN；非可信 LAN 应配置 HTTPS 反向代理。
WEB_BIND=0.0.0.0
WEB_PORT=8088
BLUEZ_IMAGE=dlaq/songloft-bluez-control:latest

BLUEZ_ADAPTER=hci0
SCAN_SECONDS=20
